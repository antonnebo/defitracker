<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="/icon.png" sizes="512x512">
  <link rel="icon" type="image/svg+xml" href="/icon.svg">
  <link rel="apple-touch-icon" href="/icon.png">
  <title>DeFi Tracker - Весь ваш криптопортфель в одном приложении</title>

  <!-- Distinctive Cyrillic-optimized fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@400;600;800&family=Onest:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    /* ========================================
       CSS VARIABLES - Premium Fintech Theme
       ======================================== */
    :root {
      /* Colors - Dark Mode First */
      --color-bg-primary: #0A0A0A;
      --color-bg-secondary: #1A1A1A;
      --color-bg-tertiary: #252525;
      --color-bg-elevated: #2A2A2A;

      /* Accent - Sophisticated Emerald/Teal */
      --color-accent-primary: #10B981;
      --color-accent-secondary: #14F195;
      --color-accent-dark: #059669;

      /* Text Colors */
      --color-text-primary: #F5F5F5;
      --color-text-secondary: #A3A3A3;
      --color-text-tertiary: #737373;

      /* Borders */
      --color-border: rgba(255, 255, 255, 0.08);
      --color-border-hover: rgba(255, 255, 255, 0.15);

      /* Typography */
      --font-display: 'Unbounded', sans-serif;
      --font-body: 'Onest', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;

      /* Spacing */
      --spacing-xs: 8px;
      --spacing-sm: 16px;
      --spacing-md: 24px;
      --spacing-lg: 40px;
      --spacing-xl: 64px;
      --spacing-2xl: 100px;
      --spacing-3xl: 150px;

      /* Shadows */
      --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.4);
      --shadow-md: 0 4px 24px rgba(0, 0, 0, 0.5);
      --shadow-lg: 0 8px 48px rgba(0, 0, 0, 0.6);
      --shadow-accent: 0 8px 32px rgba(16, 185, 129, 0.25);

      /* Border Radius */
      --radius-sm: 8px;
      --radius-md: 16px;
      --radius-lg: 24px;
      --radius-full: 9999px;
    }

    /* ========================================
       BASE STYLES
       ======================================== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: var(--font-body);
      line-height: 1.6;
      color: var(--color-text-primary);
      background: var(--color-bg-primary);
      overflow-x: hidden;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    /* Grain overlay for atmospheric texture */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E");
      pointer-events: none;
      opacity: 0.4;
      z-index: 1;
    }

    /* Container */
    .container {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 var(--spacing-md);
      position: relative;
      z-index: 2;
    }

    /* ========================================
       NAVIGATION HEADER
       ======================================== */
    .main-nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      background: rgba(10, 10, 10, 0.8);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--color-border);
      z-index: 1000;
      padding: var(--spacing-sm) 0;
    }

    .nav-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .nav-logo a {
      font-family: var(--font-display);
      font-size: 1.3rem;
      font-weight: 800;
      color: var(--color-accent-secondary);
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .nav-logo a:hover {
      color: var(--color-accent-primary);
    }

    .nav-buttons {
      display: flex;
      gap: var(--spacing-sm);
      align-items: center;
    }

    .nav-btn-login {
      padding: 10px 24px;
      color: var(--color-text-primary);
      text-decoration: none;
      font-weight: 600;
      font-size: 0.95rem;
      transition: color 0.3s ease;
      border-radius: var(--radius-full);
    }

    .nav-btn-login:hover {
      color: var(--color-accent-primary);
    }

    .nav-btn-signup {
      padding: 10px 24px;
      background: var(--color-accent-primary);
      color: var(--color-bg-primary);
      text-decoration: none;
      font-weight: 700;
      font-size: 0.95rem;
      border-radius: var(--radius-full);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 16px rgba(16, 185, 129, 0.25);
    }

    .nav-btn-signup:hover {
      background: var(--color-accent-secondary);
      transform: translateY(-2px);
      box-shadow: 0 6px 24px rgba(16, 185, 129, 0.4);
    }

    /* ========================================
       HERO SECTION - Asymmetric Layout
       ======================================== */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: calc(var(--spacing-2xl) + 80px) 0 var(--spacing-2xl);
      overflow: hidden;
    }

    /* Gradient mesh background - NOT purple */
    .hero::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -20%;
      width: 800px;
      height: 800px;
      background: radial-gradient(circle, rgba(16, 185, 129, 0.15) 0%, transparent 70%);
      filter: blur(80px);
      pointer-events: none;
      animation: float 20s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translate(0, 0); }
      50% { transform: translate(-50px, 50px); }
    }

    .hero-content {
      display: grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: var(--spacing-xl);
      align-items: center;
    }

    .hero-text {
      padding-right: var(--spacing-lg);
    }

    .hero h1 {
      font-family: var(--font-display);
      font-size: clamp(2.5rem, 6vw, 4.5rem);
      font-weight: 800;
      line-height: 1.1;
      margin-bottom: var(--spacing-lg);
      letter-spacing: -0.02em;
      animation: fadeInUp 0.8s ease-out;
    }

    .hero h1 .highlight {
      color: var(--color-accent-secondary);
      position: relative;
      display: inline-block;
    }

    .hero .subtitle {
      font-size: clamp(1.1rem, 2vw, 1.4rem);
      color: var(--color-text-secondary);
      margin-bottom: var(--spacing-xl);
      line-height: 1.7;
      font-weight: 400;
      animation: fadeInUp 0.8s ease-out 0.2s backwards;
    }

    .hero-ctas {
      display: flex;
      gap: var(--spacing-sm);
      flex-wrap: wrap;
      animation: fadeInUp 0.8s ease-out 0.4s backwards;
    }

    .cta-primary {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-xs);
      background: var(--color-accent-primary);
      color: var(--color-bg-primary);
      padding: 18px 36px;
      border-radius: var(--radius-full);
      text-decoration: none;
      font-weight: 700;
      font-size: 1.1rem;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: var(--shadow-accent);
      border: 2px solid transparent;
    }

    .cta-primary:hover {
      background: var(--color-accent-secondary);
      transform: translateY(-2px);
      box-shadow: 0 12px 48px rgba(16, 185, 129, 0.4);
    }

    .cta-secondary {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-xs);
      background: transparent;
      color: var(--color-text-primary);
      padding: 18px 36px;
      border-radius: var(--radius-full);
      text-decoration: none;
      font-weight: 600;
      font-size: 1.1rem;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      border: 2px solid var(--color-border);
    }

    .cta-secondary:hover {
      border-color: var(--color-border-hover);
      background: var(--color-bg-secondary);
    }

    /* Hero visual - asymmetric card */
    .hero-visual {
      position: relative;
      animation: fadeInUp 0.8s ease-out 0.6s backwards;
    }

    .hero-card {
      background: var(--color-bg-secondary);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: var(--spacing-lg);
      backdrop-filter: blur(20px);
      box-shadow: var(--shadow-lg);
      transform: rotate(-2deg);
      transition: transform 0.4s ease;
    }

    .hero-card:hover {
      transform: rotate(0deg) translateY(-8px);
    }

    .portfolio-preview {
      margin-bottom: var(--spacing-md);
    }

    .portfolio-preview h3 {
      font-size: 0.875rem;
      color: var(--color-text-tertiary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: var(--spacing-sm);
      font-weight: 600;
    }

    .portfolio-value {
      font-family: var(--font-display);
      font-size: 3rem;
      font-weight: 800;
      color: var(--color-accent-secondary);
      margin-bottom: var(--spacing-xs);
      line-height: 1;
    }

    .portfolio-change {
      font-size: 1.1rem;
      color: var(--color-accent-primary);
      font-weight: 600;
    }

    .portfolio-assets {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-sm);
      margin-top: var(--spacing-md);
      padding-top: var(--spacing-md);
      border-top: 1px solid var(--color-border);
    }

    .asset-item {
      text-align: center;
    }

    .asset-label {
      font-size: 0.75rem;
      color: var(--color-text-tertiary);
      margin-bottom: 4px;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .asset-value {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--color-text-primary);
    }

    /* ========================================
       TRUST BAR - Security & Stats
       ======================================== */
    .trust-bar {
      background: var(--color-bg-secondary);
      border-top: 1px solid var(--color-border);
      border-bottom: 1px solid var(--color-border);
      padding: var(--spacing-lg) 0;
      margin-bottom: var(--spacing-3xl);
    }

    .trust-items {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--spacing-lg);
      text-align: center;
    }

    .trust-item {
      opacity: 0;
      animation: fadeInUp 0.6s ease-out forwards;
    }

    .trust-item:nth-child(1) { animation-delay: 0.1s; }
    .trust-item:nth-child(2) { animation-delay: 0.2s; }
    .trust-item:nth-child(3) { animation-delay: 0.3s; }
    .trust-item:nth-child(4) { animation-delay: 0.4s; }

    .trust-icon {
      font-size: 2rem;
      margin-bottom: var(--spacing-xs);
      display: block;
    }

    .trust-label {
      font-size: 0.875rem;
      color: var(--color-text-secondary);
      display: block;
      margin-bottom: 4px;
    }

    .trust-value {
      font-weight: 700;
      color: var(--color-accent-secondary);
      font-size: 1.1rem;
      font-family: var(--font-display);
    }

    /* ========================================
       CORE FEATURES SECTION
       ======================================== */
    .features {
      padding: var(--spacing-3xl) 0;
      position: relative;
    }

    .section-header {
      text-align: center;
      max-width: 800px;
      margin: 0 auto var(--spacing-2xl);
    }

    .section-label {
      font-size: 0.875rem;
      color: var(--color-accent-primary);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
      display: block;
    }

    .section-title {
      font-family: var(--font-display);
      font-size: clamp(2rem, 4vw, 3.5rem);
      font-weight: 800;
      line-height: 1.2;
      margin-bottom: var(--spacing-md);
      letter-spacing: -0.02em;
    }

    .section-description {
      font-size: 1.2rem;
      color: var(--color-text-secondary);
      line-height: 1.7;
    }

    /* Grid-breaking layout with overlap */
    .features-grid {
      display: grid;
      grid-template-columns: repeat(12, 1fr);
      gap: var(--spacing-lg);
      margin-bottom: var(--spacing-2xl);
    }

    .feature-card {
      background: var(--color-bg-secondary);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: var(--spacing-xl);
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .feature-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--color-accent-primary), var(--color-accent-secondary));
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .feature-card:hover {
      transform: translateY(-8px);
      border-color: var(--color-border-hover);
      box-shadow: var(--shadow-lg);
    }

    .feature-card:hover::before {
      opacity: 1;
    }

    .feature-card:nth-child(1) {
      grid-column: 1 / 7;
    }

    .feature-card:nth-child(2) {
      grid-column: 7 / 13;
      margin-top: var(--spacing-xl);
    }

    .feature-card:nth-child(3) {
      grid-column: 1 / 13;
    }

    .feature-icon {
      width: 56px;
      height: 56px;
      background: rgba(16, 185, 129, 0.1);
      border-radius: var(--radius-md);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.8rem;
      margin-bottom: var(--spacing-md);
    }

    .feature-card h3 {
      font-family: var(--font-display);
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
      line-height: 1.3;
    }

    .feature-card p {
      color: var(--color-text-secondary);
      line-height: 1.7;
      font-size: 1.05rem;
    }

    /* Interactive Demo Embed */
    .demo-embed {
      background: var(--color-bg-elevated);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: var(--spacing-xl);
      margin-top: var(--spacing-2xl);
      position: relative;
      overflow: hidden;
    }

    .demo-embed::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--color-accent-primary), transparent);
    }

    .demo-header {
      text-align: center;
      margin-bottom: var(--spacing-lg);
    }

    .demo-header h3 {
      font-family: var(--font-display);
      font-size: 1.8rem;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
    }

    .demo-preview {
      background: var(--color-bg-secondary);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-md);
      padding: var(--spacing-lg);
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: var(--spacing-md);
    }

    .demo-stat {
      text-align: center;
      padding: var(--spacing-md);
      background: var(--color-bg-primary);
      border-radius: var(--radius-sm);
      border: 1px solid var(--color-border);
    }

    .demo-stat-label {
      font-size: 0.875rem;
      color: var(--color-text-tertiary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: var(--spacing-xs);
    }

    .demo-stat-value {
      font-family: var(--font-display);
      font-size: 2rem;
      font-weight: 800;
      color: var(--color-accent-secondary);
    }

    .demo-cta {
      text-align: center;
      margin-top: var(--spacing-lg);
    }

    /* ========================================
       SOCIAL PROOF SECTION
       ======================================== */
    .social-proof {
      padding: var(--spacing-3xl) 0;
      background: var(--color-bg-secondary);
      border-top: 1px solid var(--color-border);
      border-bottom: 1px solid var(--color-border);
    }

    .testimonials-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: var(--spacing-lg);
      margin-bottom: var(--spacing-2xl);
    }

    .testimonial-card {
      background: var(--color-bg-primary);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: var(--spacing-lg);
      position: relative;
      transition: all 0.3s ease;
    }

    .testimonial-card:hover {
      border-color: var(--color-border-hover);
      transform: translateY(-4px);
    }

    .testimonial-quote {
      font-size: 1.05rem;
      line-height: 1.7;
      color: var(--color-text-primary);
      margin-bottom: var(--spacing-md);
      font-style: italic;
    }

    .testimonial-author {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
    }

    .author-avatar {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--color-accent-primary), var(--color-accent-secondary));
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
    }

    .author-info {
      flex: 1;
    }

    .author-name {
      font-weight: 700;
      color: var(--color-text-primary);
      margin-bottom: 2px;
    }

    .author-role {
      font-size: 0.875rem;
      color: var(--color-text-tertiary);
    }

    /* Discord Community CTA */
    .community-cta {
      background: var(--color-bg-elevated);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: var(--spacing-xl);
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .community-cta::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(16, 185, 129, 0.1) 0%, transparent 70%);
      animation: rotate 20s linear infinite;
    }

    @keyframes rotate {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .community-cta-content {
      position: relative;
      z-index: 2;
    }

    .discord-icon {
      font-size: 3.5rem;
      margin-bottom: var(--spacing-md);
      display: block;
    }

    .community-cta h3 {
      font-family: var(--font-display);
      font-size: 1.8rem;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
    }

    .community-cta p {
      color: var(--color-text-secondary);
      margin-bottom: var(--spacing-lg);
      font-size: 1.1rem;
    }

    .community-stats {
      display: flex;
      justify-content: center;
      gap: var(--spacing-xl);
      margin-bottom: var(--spacing-lg);
    }

    .community-stat {
      text-align: center;
    }

    .community-stat-value {
      font-family: var(--font-display);
      font-size: 2rem;
      font-weight: 800;
      color: var(--color-accent-secondary);
      display: block;
    }

    .community-stat-label {
      font-size: 0.875rem;
      color: var(--color-text-tertiary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    /* ========================================
       PRICING SECTION
       ======================================== */
    .pricing {
      padding: var(--spacing-3xl) 0;
    }

    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: var(--spacing-lg);
      max-width: 1100px;
      margin: 0 auto;
    }

    .pricing-card {
      background: var(--color-bg-secondary);
      border: 2px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: var(--spacing-xl);
      transition: all 0.3s ease;
      position: relative;
    }

    .pricing-card:hover {
      border-color: var(--color-border-hover);
      transform: translateY(-8px);
      box-shadow: var(--shadow-lg);
    }

    .pricing-card.featured {
      border-color: var(--color-accent-primary);
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.05), transparent);
    }

    .pricing-card.featured::before {
      content: 'Популярный';
      position: absolute;
      top: -12px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--color-accent-primary);
      color: var(--color-bg-primary);
      padding: 4px 16px;
      border-radius: var(--radius-full);
      font-size: 0.75rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .pricing-tier {
      font-size: 0.875rem;
      color: var(--color-accent-primary);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
    }

    .pricing-price {
      font-family: var(--font-display);
      font-size: 3rem;
      font-weight: 800;
      line-height: 1;
      margin-bottom: var(--spacing-xs);
    }

    .pricing-period {
      color: var(--color-text-tertiary);
      font-size: 1rem;
      margin-bottom: var(--spacing-lg);
    }

    .pricing-features {
      list-style: none;
      margin-bottom: var(--spacing-lg);
    }

    .pricing-features li {
      padding: var(--spacing-sm) 0;
      color: var(--color-text-secondary);
      display: flex;
      align-items: flex-start;
      gap: var(--spacing-sm);
      line-height: 1.6;
    }

    .pricing-features li::before {
      content: '✓';
      color: var(--color-accent-primary);
      font-weight: 700;
      flex-shrink: 0;
    }

    .pricing-cta {
      display: block;
      width: 100%;
      padding: 16px;
      border-radius: var(--radius-full);
      text-align: center;
      text-decoration: none;
      font-weight: 700;
      transition: all 0.3s ease;
      border: 2px solid var(--color-border);
      background: transparent;
      color: var(--color-text-primary);
    }

    .pricing-cta:hover {
      border-color: var(--color-accent-primary);
      background: rgba(16, 185, 129, 0.1);
    }

    .pricing-card.featured .pricing-cta {
      background: var(--color-accent-primary);
      color: var(--color-bg-primary);
      border-color: var(--color-accent-primary);
    }

    .pricing-card.featured .pricing-cta:hover {
      background: var(--color-accent-secondary);
      border-color: var(--color-accent-secondary);
    }

    /* ========================================
       FINAL CTA SECTION
       ======================================== */
    .final-cta {
      padding: var(--spacing-3xl) 0;
      background: var(--color-bg-secondary);
      border-top: 1px solid var(--color-border);
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .final-cta::before {
      content: '';
      position: absolute;
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, rgba(16, 185, 129, 0.1) 0%, transparent 70%);
      pointer-events: none;
    }

    .final-cta-content {
      position: relative;
      z-index: 2;
      max-width: 700px;
      margin: 0 auto;
    }

    .final-cta h2 {
      font-family: var(--font-display);
      font-size: clamp(2rem, 4vw, 3.5rem);
      font-weight: 800;
      line-height: 1.2;
      margin-bottom: var(--spacing-md);
      letter-spacing: -0.02em;
    }

    .final-cta-description {
      font-size: 1.2rem;
      color: var(--color-text-secondary);
      margin-bottom: var(--spacing-xl);
      line-height: 1.7;
    }

    .final-cta-buttons {
      display: flex;
      gap: var(--spacing-sm);
      justify-content: center;
      flex-wrap: wrap;
      margin-bottom: var(--spacing-xl);
    }

    .final-cta-note {
      font-size: 0.95rem;
      color: var(--color-text-tertiary);
      margin-bottom: var(--spacing-2xl);
    }

    .security-badges {
      display: flex;
      justify-content: center;
      gap: var(--spacing-lg);
      flex-wrap: wrap;
      padding-top: var(--spacing-xl);
      border-top: 1px solid var(--color-border);
    }

    .security-badge {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
      font-size: 0.95rem;
      color: var(--color-text-secondary);
    }

    .security-badge-icon {
      color: var(--color-accent-primary);
    }

    /* ========================================
       ANIMATIONS
       ======================================== */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Scroll reveal animation */
    .scroll-reveal {
      opacity: 0;
      transform: translateY(40px);
      transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .scroll-reveal.revealed {
      opacity: 1;
      transform: translateY(0);
    }

    /* ========================================
       RESPONSIVE - Mobile First
       ======================================== */
    @media (max-width: 968px) {
      :root {
        --spacing-2xl: 60px;
        --spacing-3xl: 80px;
      }

      .hero-content {
        grid-template-columns: 1fr;
        gap: var(--spacing-xl);
      }

      .hero-text {
        padding-right: 0;
        text-align: center;
      }

      .hero-ctas {
        justify-content: center;
      }

      .hero-visual {
        order: -1;
      }

      .hero-card {
        transform: rotate(0deg);
      }

      .features-grid .feature-card {
        grid-column: 1 / 13 !important;
        margin-top: 0 !important;
      }

      .trust-items {
        grid-template-columns: repeat(2, 1fr);
      }

      .pricing-grid {
        grid-template-columns: 1fr;
        max-width: 400px;
      }

      .community-stats {
        flex-direction: column;
        gap: var(--spacing-md);
      }
    }

    @media (max-width: 640px) {
      .nav-logo a {
        font-size: 1.1rem;
      }

      .nav-btn-login,
      .nav-btn-signup {
        padding: 8px 16px;
        font-size: 0.875rem;
      }

      .nav-buttons {
        gap: 8px;
      }

      .hero h1 {
        font-size: 2.2rem;
      }

      .portfolio-value {
        font-size: 2.5rem;
      }

      .portfolio-assets {
        grid-template-columns: 1fr;
      }

      .hero-ctas {
        flex-direction: column;
        width: 100%;
      }

      .cta-primary,
      .cta-secondary {
        width: 100%;
        justify-content: center;
      }

      .trust-items {
        grid-template-columns: 1fr;
      }

      .final-cta-buttons {
        flex-direction: column;
        width: 100%;
      }

      .security-badges {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>
<body>

  <!-- ========================================
       NAVIGATION HEADER
       ======================================== -->
  <nav class="main-nav">
    <div class="container">
      <div class="nav-content">
        <div class="nav-logo">
          <a href="/">DeFi Tracker</a>
        </div>
        <div class="nav-buttons">
          <%= link_to "Войти", new_session_path, class: "nav-btn-login" %>
          <%= link_to "Регистрация", new_registration_path, class: "nav-btn-signup" %>
        </div>
      </div>
    </div>
  </nav>

  <!-- ========================================
       HERO SECTION
       ======================================== -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <div class="hero-text">
          <h1>
            Весь ваш <span class="highlight">крипто&shy;портфель</span> в одном приложении
          </h1>
          <p class="subtitle">
            Единственное место, где вы видите все кошельки, биржи и DeFi-позиции одновременно.
            Мобильный опыт премиум-класса для активных крипто-пользователей.
          </p>
          <div class="hero-ctas">
            <%= link_to "Начать бесплатно", new_registration_path, class: "cta-primary" %>
            <a href="#demo" class="cta-secondary">
              Посмотреть демо
            </a>
          </div>
        </div>

        <div class="hero-visual">
          <div class="hero-card">
            <div class="portfolio-preview">
              <h3>Портфель</h3>
              <div class="portfolio-value">$127,453</div>
              <div class="portfolio-change">+$12,847 (11.2%) за месяц</div>
            </div>
            <div class="portfolio-assets">
              <div class="asset-item">
                <div class="asset-label">Кошельки</div>
                <div class="asset-value">8</div>
              </div>
              <div class="asset-item">
                <div class="asset-label">Биржи</div>
                <div class="asset-value">4</div>
              </div>
              <div class="asset-item">
                <div class="asset-label">Сети</div>
                <div class="asset-value">12</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================================
       TRUST BAR
       ======================================== -->
  <section class="trust-bar">
    <div class="container">
      <div class="trust-items">
        <div class="trust-item">
          <span class="trust-icon">🔒</span>
          <span class="trust-label">Безопасность</span>
          <span class="trust-value">Read-only API</span>
        </div>
        <div class="trust-item">
          <span class="trust-icon">⚡</span>
          <span class="trust-label">Обновления</span>
          <span class="trust-value">Реал-тайм</span>
        </div>
        <div class="trust-item">
          <span class="trust-icon">🌐</span>
          <span class="trust-label">Поддержка</span>
          <span class="trust-value">30+ сетей</span>
        </div>
        <div class="trust-item">
          <span class="trust-icon">🔌</span>
          <span class="trust-label">Протоколы</span>
          <span class="trust-value">400+ DeFi</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================================
       CORE FEATURES
       ======================================== -->
  <section class="features">
    <div class="container">
      <div class="section-header scroll-reveal">
        <span class="section-label">Возможности</span>
        <h2 class="section-title">Полный контроль над портфелем</h2>
        <p class="section-description">
          Три ключевых преимущества, которые выделяют нас среди конкурентов
        </p>
      </div>

      <div class="features-grid">
        <div class="feature-card scroll-reveal">
          <div class="feature-icon">🔗</div>
          <h3>CEX + DeFi + NFT в одном месте</h3>
          <p>
            Видите балансы всех бирж (Binance, Coinbase, Bybit), кошельков и DeFi-позиций
            одновременно. Больше не нужно переключаться между 5+ приложениями.
          </p>
        </div>

        <div class="feature-card scroll-reveal">
          <div class="feature-icon">📱</div>
          <h3>Премиум мобильный опыт</h3>
          <p>
            Нативные iOS и Android приложения, оптимизированные для OLED экранов.
            Проверяйте портфель за 5 секунд, принимайте решения на ходу.
          </p>
        </div>

        <div class="feature-card scroll-reveal">
          <div class="feature-icon">📊</div>
          <h3>Реальная аналитика и P&L</h3>
          <p>
            Точный расчёт доходности по каждому активу, цепочке и стратегии.
            Подсказки по оптимизации, алерты о важных изменениях, умные рекомендации.
          </p>
        </div>
      </div>

      <!-- Interactive Demo -->
      <div class="demo-embed scroll-reveal" id="demo">
        <div class="demo-header">
          <h3>Увидьте свой портфель за 60 секунд</h3>
          <p style="color: var(--color-text-secondary); font-size: 1.05rem;">
            Подключите кошелёк или вставьте адрес — мгновенная загрузка всех активов
          </p>
        </div>

        <div class="demo-preview">
          <div class="demo-stat">
            <div class="demo-stat-label">Время настройки</div>
            <div class="demo-stat-value">60 сек</div>
          </div>
          <div class="demo-stat">
            <div class="demo-stat-label">Кошельков бесплатно</div>
            <div class="demo-stat-value">5</div>
          </div>
          <div class="demo-stat">
            <div class="demo-stat-label">Бирж бесплатно</div>
            <div class="demo-stat-value">2</div>
          </div>
          <div class="demo-stat">
            <div class="demo-stat-label">Экономия времени</div>
            <div class="demo-stat-value">8+ ч/мес</div>
          </div>
        </div>

        <div class="demo-cta">
          <%= link_to "Попробовать демо", new_registration_path, class: "cta-primary", style: "display: inline-flex;" %>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================================
       SOCIAL PROOF
       ======================================== -->
  <section class="social-proof">
    <div class="container">
      <div class="section-header scroll-reveal">
        <span class="section-label">Отзывы</span>
        <h2 class="section-title">Что говорят пользователи</h2>
      </div>

      <div class="testimonials-grid">
        <div class="testimonial-card scroll-reveal">
          <p class="testimonial-quote">
            "Наконец-то вижу полную картину портфеля. Раньше тратил час каждое утро,
            проверяя 6 разных приложений. Теперь — 10 секунд."
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">🧑</div>
            <div class="author-info">
              <div class="author-name">Алексей М.</div>
              <div class="author-role">DeFi-трейдер, 3 года опыта</div>
            </div>
          </div>
        </div>

        <div class="testimonial-card scroll-reveal">
          <p class="testimonial-quote">
            "Мобильное приложение работает невероятно быстро. Могу проверить портфель
            в любой момент, даже в метро. Незаменимо для активной торговли."
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">👩</div>
            <div class="author-info">
              <div class="author-name">Мария К.</div>
              <div class="author-role">NFT коллекционер</div>
            </div>
          </div>
        </div>

        <div class="testimonial-card scroll-reveal">
          <p class="testimonial-quote">
            "Главное преимущество — безопасность. Только read-only доступ, никаких прав
            на вывод. Спокойно добавил все биржи и сплю спокойно."
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">👨</div>
            <div class="author-info">
              <div class="author-name">Дмитрий П.</div>
              <div class="author-role">Криптоинвестор, $250K портфель</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Discord Community -->
      <div class="community-cta scroll-reveal">
        <div class="community-cta-content">
          <span class="discord-icon">💬</span>
          <h3>Присоединяйтесь к сообществу</h3>
          <p>
            Более 10,000 активных крипто-пользователей обсуждают стратегии,
            делятся альфой и помогают друг другу в нашем Discord
          </p>

          <div class="community-stats">
            <div class="community-stat">
              <span class="community-stat-value">10,000+</span>
              <span class="community-stat-label">Участников</span>
            </div>
            <div class="community-stat">
              <span class="community-stat-value">24/7</span>
              <span class="community-stat-label">Поддержка</span>
            </div>
            <div class="community-stat">
              <span class="community-stat-value">100+</span>
              <span class="community-stat-label">Сообщений в день</span>
            </div>
          </div>

          <%= link_to "Войти в Discord", "#", class: "cta-primary", style: "display: inline-flex;" %>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================================
       PRICING
       ======================================== -->
  <section class="pricing">
    <div class="container">
      <div class="section-header scroll-reveal">
        <span class="section-label">Тарифы</span>
        <h2 class="section-title">Прозрачные цены</h2>
        <p class="section-description">
          Начните бесплатно, переходите на Pro по мере роста портфеля
        </p>
      </div>

      <div class="pricing-grid">
        <div class="pricing-card scroll-reveal">
          <div class="pricing-tier">Free</div>
          <div class="pricing-price">$0</div>
          <div class="pricing-period">Навсегда бесплатно</div>
          <ul class="pricing-features">
            <li>5 кошельков</li>
            <li>2 биржи</li>
            <li>Базовая аналитика</li>
            <li>Обновления каждые 30 мин</li>
            <li>Веб-доступ</li>
          </ul>
          <%= link_to "Начать бесплатно", new_registration_path, class: "pricing-cta" %>
        </div>

        <div class="pricing-card featured scroll-reveal">
          <div class="pricing-tier">Plus</div>
          <div class="pricing-price">$9.99</div>
          <div class="pricing-period">в месяц</div>
          <ul class="pricing-features">
            <li>15 кошельков</li>
            <li>5 бирж</li>
            <li>Реал-тайм обновления (30 сек)</li>
            <li>Мобильное приложение</li>
            <li>Расширенная аналитика</li>
            <li>P&L трекинг</li>
          </ul>
          <%= link_to "Подключить Plus", new_registration_path, class: "pricing-cta" %>
        </div>

        <div class="pricing-card scroll-reveal">
          <div class="pricing-tier">Pro</div>
          <div class="pricing-price">$29.99</div>
          <div class="pricing-period">в месяц</div>
          <ul class="pricing-features">
            <li>50 кошельков</li>
            <li>Неограниченно бирж</li>
            <li>Реал-тайм обновления (5 сек)</li>
            <li>Продвинутая аналитика</li>
            <li>Налоговые отчёты</li>
            <li>API доступ</li>
            <li>Приоритетная поддержка</li>
          </ul>
          <%= link_to "Подключить Pro", new_registration_path, class: "pricing-cta" %>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================================
       FINAL CTA
       ======================================== -->
  <section class="final-cta">
    <div class="container">
      <div class="final-cta-content scroll-reveal">
        <h2>Начните управлять портфелем правильно</h2>
        <p class="final-cta-description">
          Присоединяйтесь к тысячам активных крипто-пользователей, которые экономят
          8+ часов в месяц и принимают лучшие инвестиционные решения
        </p>

        <div class="final-cta-buttons">
          <%= link_to "Создать аккаунт бесплатно", new_registration_path, class: "cta-primary" %>
          <a href="#demo" class="cta-secondary">
            Посмотреть демо
          </a>
        </div>

        <p class="final-cta-note">
          ✓ Без кредитной карты  ✓ Настройка за 60 секунд  ✓ 5 кошельков бесплатно навсегда
        </p>

        <div class="security-badges">
          <div class="security-badge">
            <span class="security-badge-icon">🔒</span>
            <span>Только read-only доступ</span>
          </div>
          <div class="security-badge">
            <span class="security-badge-icon">🛡️</span>
            <span>Шифрование AES-256</span>
          </div>
          <div class="security-badge">
            <span class="security-badge-icon">🚫</span>
            <span>Никаких прав на вывод</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================================
       SCROLL REVEAL SCRIPT
       ======================================== -->
  <script>
    // Intersection Observer for scroll reveals
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('revealed');
        }
      });
    }, observerOptions);

    document.querySelectorAll('.scroll-reveal').forEach(el => {
      observer.observe(el);
    });
  </script>

</body>
</html>
